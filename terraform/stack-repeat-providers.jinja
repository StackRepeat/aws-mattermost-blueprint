## Generated by the platform runtime. Do not add concrete account values here. ##

provider "aws" {
  region = "{{ provider_region }}"

  assume_role {
    role_arn = "{{ target_admin_role_arn }}"
  }
}
